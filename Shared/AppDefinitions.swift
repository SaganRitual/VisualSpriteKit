// We are a way for the cosmos to know itself. -- C. Sagan

import CoreGraphics

enum AppDefinitions {
    static let gameSceneSide = 2048.0
    static let gameSceneSize = CGSize(width: gameSceneSide, height: gameSceneSide)
}
